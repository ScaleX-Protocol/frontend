name: Base Sepolia Frontend CI/CD

on:
  push:
    branches: [base-sepolia]
    paths:
      - 'apps/web/**'
      - 'packages/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'Dockerfile'
      - 'nginx.conf'
      - 'turbo.json'
      - '.github/**'
  pull_request:
    branches: [base-sepolia]
    paths:
      - 'apps/web/**'
      - 'packages/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'Dockerfile'
      - 'nginx.conf'
      - 'turbo.json'
      - '.github/**'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      chain: base-sepolia
      domain: app.scalex.money
      environment: base-sepolia
    secrets:
      VAULT_ADDR: ${{ secrets.VAULT_ADDR }}
      VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}
      BASE_SEPOLIA: ${{ secrets.BASE_SEPOLIA }}
