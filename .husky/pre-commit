#!/usr/bin/env sh

# Run lint on staged files for performance
echo "🔍 Running pre-commit lint check on staged files..."
pnpm lint-staged

# Run full typecheck to catch TypeScript errors (needed for full codebase)
echo "🔍 Running pre-commit type check..."
pnpm run typecheck
