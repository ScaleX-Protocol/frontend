services:
  # Base Sepolia Frontend Application
  frontend-base-sepolia:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CHAIN: base-sepolia
    container_name: scalex-frontend-base-sepolia
    restart: unless-stopped
    # No port exposure needed - Traefik handles routing by domain
    networks:
      - infrastructure
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    labels:
      # Traefik configuration
      - "traefik.enable=true"
      - "traefik.http.services.frontend-base-sepolia.loadbalancer.server.port=80"
      - "traefik.http.routers.frontend-base-sepolia.rule=Host(`base-sepolia-app.scalex.money`)"
      - "traefik.http.routers.frontend-base-sepolia.entrypoints=websecure"
      - "traefik.http.routers.frontend-base-sepolia.tls.certresolver=letsencrypt"
      - "traefik.http.routers.frontend-base-sepolia.service=frontend-base-sepolia"

networks:
  infrastructure:
    external: true