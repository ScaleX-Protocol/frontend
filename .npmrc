# Hoist zod to the root so all packages can access it
public-hoist-pattern[]=*zod*
public-hoist-pattern[]=*@noble*
public-hoist-pattern[]=*@privy-io*

# Shamefully hoist everything for React Native compatibility
shamefully-hoist=true
